local fruits = { "apple", "banana", "orange" }

for i, fruit in ipairs(fruits) do
    print(i, fruit)
end

local person = {
    name = "Lazaro",
    age = 20
}

print(person.name, person.age)
